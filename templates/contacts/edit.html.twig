{% extends "base.html.twig" %}

{% block body %}

    {% form_theme form 'form_table_layout.html.twig' %}

    <a href="{{ path('contacts_index') }}" class=""><< Contacts</a>

    <fieldset>
        <legend>Edit Contact</legend>
        {{ form(form, {'attr': {'novalidate': 'novalidate'}}) }}
    </fieldset>

{% endblock %}
