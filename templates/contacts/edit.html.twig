{% form_theme form 'form_table_layout.html.twig' %}

<a href="{{ path('contacts_index') }}"><< Contacts</a> <br>
<fieldset>
    <legend>Edit Contact</legend>
    {{ form(form, {'attr': {'novalidate': 'novalidate'}}) }}
</fieldset>
